import types from './types'
import { actions } from './actions'
import reducer, { selectors } from './reducer'

export default reducer

export {
  types as collapseTypes,
  actions as collapseActions,
  selectors as collapseSelectors,
}
